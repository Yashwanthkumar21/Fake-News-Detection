# Fake-News-Detection
A Fake News Detection system using BERT that classifies news articles as real or fake. The model is fine-tuned on a labeled dataset and evaluated using accuracy, precision, recall, and F1-score.
